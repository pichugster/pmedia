---
---
<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>pichugster — удалённый доход и жизнь без границ</title>
<meta name="description" content="Как создать удалённый доход с нейросетями, монтажом и цифровыми навыками — и жить где хочешь. Флагман — Вьетнам.">
<link rel="icon" href="/favicon.png">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;500;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<style is:global>
  :root{
    --midnight:#0F172A; --sky:#2563EB; --cyan:#06B6D4; --violet:#8B5CF6;
    --coral:#FF7E6B; --pink:#FB7BA2; --cloud:#F8FAFC; --slate:#64748B; --line:#E8ECF3;
    --grad:linear-gradient(135deg,#06B6D4 0%,#5B7CF0 40%,#B36BE0 70%,#FF8FA8 100%);
    --display:"Quicksand",system-ui,sans-serif; --body:"Inter",system-ui,sans-serif;
    --maxw:1140px;
  }
  *{box-sizing:border-box;margin:0;padding:0}
  html{scroll-behavior:smooth}
  body{background:var(--cloud);color:var(--midnight);font-family:var(--body);line-height:1.6;-webkit-font-smoothing:antialiased;overflow-x:hidden}
  ::selection{background:var(--coral);color:#fff}
  a{color:inherit;text-decoration:none}
  img{max-width:100%;display:block}
  .wrap{max-width:var(--maxw);margin:0 auto;padding:0 28px}
  :focus-visible{outline:2px solid var(--sky);outline-offset:3px;border-radius:6px}
  .eyebrow{font-size:.74rem;font-weight:600;letter-spacing:.24em;text-transform:uppercase}
  .sec-label{font-size:.74rem;font-weight:600;letter-spacing:.22em;text-transform:uppercase;color:var(--sky);margin-bottom:10px}
  .sec-title{font-family:var(--display);font-weight:600;font-size:clamp(1.7rem,3.4vw,2.6rem);line-height:1.08;letter-spacing:-.015em}
  .sec-intro{color:var(--slate);max-width:60ch;margin-top:10px;font-size:1.05rem}
  .band{padding:80px 0}
  .center{text-align:center;margin:0 auto}

  .btn{font-family:var(--display);font-weight:600;font-size:.95rem;padding:14px 26px;border-radius:999px;display:inline-flex;align-items:center;gap:.5em;transition:transform .25s,box-shadow .3s;cursor:pointer;border:0}
  .btn-primary{background:var(--sky);color:#fff}
  .btn-primary:hover{transform:translateY(-2px);box-shadow:0 16px 36px -12px rgba(37,99,235,.6)}
  .btn-ghost{border:1.5px solid rgba(255,255,255,.55);color:#fff;background:rgba(255,255,255,.08)}
  .btn-ghost:hover{transform:translateY(-2px);border-color:#fff}
  .btn-ghost.dark{border:1.5px solid var(--line);color:var(--midnight);background:#fff}
  .cta-row{display:flex;flex-wrap:wrap;gap:14px}

  nav{position:sticky;top:0;z-index:50;display:flex;align-items:center;justify-content:space-between;gap:20px;padding:12px 28px;background:rgba(248,250,252,.82);backdrop-filter:blur(12px);border-bottom:1px solid var(--line)}
  .brand{display:flex;align-items:center;gap:10px}
  .brand img{height:34px;width:auto}
  .brand b{font-family:var(--display);font-weight:700;font-size:1.15rem;letter-spacing:-.01em}
  .nav-links{display:flex;gap:22px;font-size:.92rem;color:var(--slate)}
  .nav-links a:hover{color:var(--midnight)}
  .nav-cta{font-family:var(--display);font-weight:600;font-size:.86rem;background:var(--sky);color:#fff;padding:9px 18px;border-radius:999px;white-space:nowrap}
  .burger{display:none;background:none;border:0;font-size:1.4rem;cursor:pointer;color:var(--midnight)}

  .hero{position:relative;min-height:82vh;display:flex;align-items:center;color:#fff;overflow:hidden}
  .hero-bg{position:absolute;inset:0;z-index:0;background:url('/img/hero.webp') 62% center/cover}
  .hero-bg::after{content:"";position:absolute;inset:0;background:linear-gradient(90deg,rgba(15,23,42,.74) 0%,rgba(15,23,42,.4) 42%,transparent 72%)}
  .hero .wrap{position:relative;z-index:5;padding:70px 28px}
  .hero .eyebrow{color:#cfeaf2}.hero .eyebrow b{color:var(--coral)}
  .hero h1{font-family:var(--display);font-weight:700;font-size:clamp(2.4rem,6vw,4.4rem);line-height:1.03;letter-spacing:-.02em;max-width:17ch;margin:.4em 0 .38em}
  .hero p.lede{font-size:clamp(1.05rem,1.7vw,1.28rem);color:#e4ebf6;max-width:50ch;margin-bottom:1.6em}
  .scroll{position:absolute;left:50%;bottom:24px;transform:translateX(-50%);z-index:5;width:1px;height:32px;background:linear-gradient(#fff,transparent);opacity:.5;animation:cue 1.8s ease-in-out infinite}
  @keyframes cue{0%,100%{transform:translateX(-50%) scaleY(.4);opacity:.3}50%{transform:translateX(-50%) scaleY(1);opacity:.9}}

  .dirs{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin-top:38px}
  .dir{padding:26px 24px;border:1px solid var(--line);border-radius:18px;background:#fff;transition:transform .3s,border-color .3s,box-shadow .3s;display:block;color:inherit}
  .dir:hover{transform:translateY(-4px);border-color:#c7d2fe;box-shadow:0 20px 40px -28px rgba(37,99,235,.5)}
  .dir .ic{width:30px;height:30px;color:var(--sky);margin-bottom:2px}
  .dir h3{font-family:var(--display);font-weight:600;font-size:1.12rem;margin:12px 0 6px}
  .dir p{color:var(--slate);font-size:.93rem;margin-bottom:12px}
  .dir .go{font-family:var(--display);font-weight:600;font-size:.85rem;color:var(--sky)}

  .benefits{display:grid;grid-template-columns:repeat(2,1fr);gap:18px;margin-top:38px}
  .ben{display:flex;gap:14px;padding:22px;border:1px solid var(--line);border-radius:16px;background:#fff}
  .ben .ck{flex:none;width:40px;height:40px;border-radius:11px;background:rgba(37,99,235,.1);display:flex;align-items:center;justify-content:center;color:var(--sky)}
  .ben .ck svg{width:21px;height:21px}
  .ben h3{font-family:var(--display);font-weight:600;font-size:1.06rem;margin-bottom:3px}
  .ben p{color:var(--slate);font-size:.93rem}

  .viet{display:grid;grid-template-columns:1.1fr .9fr;gap:34px;align-items:center}
  .viet-card{border:1px solid var(--line);border-radius:20px;background:linear-gradient(160deg,rgba(6,182,212,.08),rgba(179,107,224,.07));padding:26px 28px}
  .viet-flag{font-size:2.4rem;line-height:1;margin-bottom:8px}
  .viet-facts{list-style:none}
  .viet-facts li{display:flex;justify-content:space-between;gap:16px;padding:11px 0;border-top:1px solid var(--line);font-size:.95rem}
  .viet-facts li:first-child{border-top:0}
  .viet-facts span{color:var(--slate)}
  .viet-facts b{font-family:var(--display);font-weight:600}

  .steps{display:grid;grid-template-columns:repeat(4,1fr);gap:18px;margin-top:38px}
  .step{padding:24px 20px;border-radius:16px;background:#fff;border:1px solid var(--line)}
  .step .n{font-family:var(--display);font-weight:700;font-size:1.1rem;color:var(--sky)}
  .step h3{font-family:var(--display);font-weight:600;font-size:1.05rem;margin:10px 0 6px}
  .step p{color:var(--slate);font-size:.9rem}

  .final{position:relative;color:var(--midnight);text-align:center;overflow:hidden}
  .final-bg{position:absolute;inset:0;z-index:0;background:url('/img/sky.webp') center/cover}
  .final-bg::after{content:"";position:absolute;inset:0;background:radial-gradient(58% 58% at 50% 44%,rgba(248,250,252,.55),transparent 72%)}
  .final .wrap{position:relative;z-index:5;padding:110px 28px}
  .final img.logo{width:62px;margin:0 auto 22px}
  .final h2{font-family:var(--display);font-weight:700;font-size:clamp(2rem,5vw,3.4rem);line-height:1.05;letter-spacing:-.01em;max-width:20ch;margin:0 auto .4em}
  .final h2 b{color:var(--coral)}
  .final p{color:#475569;max-width:44ch;margin:0 auto 2em}
  .final .cta-row{justify-content:center}

  footer{background:var(--midnight);color:#94A3B8;padding:46px 0}
  footer .wrap{display:flex;flex-wrap:wrap;gap:18px;justify-content:space-between;align-items:center}
  footer b{font-family:var(--display);font-weight:700;color:#fff;font-size:1.05rem}
  footer .links{display:flex;gap:18px;flex-wrap:wrap;font-size:.9rem}
  footer .meta{font-family:ui-monospace,monospace;font-size:.72rem;width:100%;border-top:1px solid rgba(255,255,255,.1);padding-top:16px}

  @media (max-width:860px){.dirs{grid-template-columns:repeat(2,1fr)}.benefits{grid-template-columns:1fr}.steps{grid-template-columns:repeat(2,1fr)}.viet{grid-template-columns:1fr;gap:22px}}
  @media (max-width:900px){.nav-links{display:none}.burger{display:block}}
  @media (max-width:600px){.nav-cta{display:none}}
  @media (max-width:560px){.dirs,.steps{grid-template-columns:1fr}.band{padding:56px 0}.wrap{padding:0 20px}}
  @media (prefers-reduced-motion:reduce){*{animation:none!important;transition:none!important;scroll-behavior:auto}}
</style>
</head>
<body>

<nav>
  <a class="brand" href="/"><img src="/logo.png" alt="pichugster"><b>pichugster</b></a>
  <div class="nav-links">
    <a href="/zarabotok">Заработок на ИИ</a><a href="/strany">Страны</a><a href="/montazh">Монтаж</a><a href="/uslugi">Услуги</a><a href="/bloger">Стать блогером</a><a href="/udalyonka">Удалёнка</a><a href="/poleznoe">Полезное</a>
  </div>
  <a class="nav-cta" href="/pichugster-ai-start.pdf" download>Забрать гайд</a>
  <button class="burger" aria-label="Меню">☰</button>
</nav>

<header class="hero">
  <div class="hero-bg"></div>
  <div class="wrap">
    <span class="eyebrow">the sky has no borders<b>.</b></span>
    <h1>Создай доход, который всегда с тобой</h1>
    <p class="lede">Пока с работой тяжело, цифровые навыки и нейросети дают то, что нельзя сократить, — доход из любой точки мира. Показываю по шагам: как освоить и куда уехать.</p>
    <div class="cta-row">
      <a class="btn btn-primary" href="#earn">Способы заработка</a>
      <a class="btn btn-ghost" href="#strany">Куда переехать</a>
    </div>
  </div>
  <div class="scroll"></div>
</header>

<section class="band" id="earn">
  <div class="wrap">
    <div class="sec-label">Способы заработка</div>
    <h2 class="sec-title">Как создать удалённый доход с нейросетями</h2>
    <p class="sec-intro">Нейросети открыли огромное поле для дохода. Вот направления, с которых можно начать — с гайдами и разборами.</p>
    <div class="dirs">
      <a class="dir" href="/zarabotok">
        <svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="7" width="16" height="12" rx="2"/><path d="M12 7V4"/><circle cx="12" cy="3" r="1"/><path d="M9 13h.01M15 13h.01"/><path d="M9.5 16.5h5"/></svg>
        <h3>Заработок на ИИ</h3><p>Услуги, контент, продукты и агенты на нейросетях.</p><span class="go">Смотреть направления →</span></a>
      <a class="dir" href="/montazh">
        <svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="16" rx="2"/><path d="M3 9h18M8 4v5M16 4v5"/><path d="M11 13l3 2-3 2z"/></svg>
        <h3>Монтаж Reels</h3><p>Монтаж видео с нейросетями — навык, который кормит.</p><span class="go">Открыть →</span></a>
      <a class="dir" href="/zarabotok/content-marketing">
        <svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4z"/></svg>
        <h3>Контент и тексты</h3><p>Статьи, сценарии, ведение — на заказ и для себя.</p><span class="go">Как зарабатывать →</span></a>
      <a class="dir" href="/zarabotok/marketplaces">
        <svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/><path d="M3 6h18"/><path d="M16 10a4 4 0 0 1-8 0"/></svg>
        <h3>Маркетплейсы</h3><p>Карточки, SEO и визуал для Wildberries и Ozon.</p><span class="go">Разобраться →</span></a>
      <a class="dir" href="/bloger">
        <svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h3l2-3h8l2 3h3a2 2 0 0 1 2 2z"/><circle cx="12" cy="13" r="4"/></svg>
        <h3>Блогинг</h3><p>Запусти блог и монетизируй аудиторию.</p><span class="go">Начать →</span></a>
      <a class="dir" href="/uslugi">
        <svg class="ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M14.7 6.3a4 4 0 0 0-5.2 5.2L4 17l3 3 5.5-5.5a4 4 0 0 0 5.2-5.2l-2.3 2.3-2.7-.7-.7-2.7z"/></svg>
        <h3>AI-услуги под ключ</h3><p>Готовые услуги с нейросетями — заказать или научиться.</p><span class="go">Смотреть →</span></a>
    </div>
  </div>
</section>

<section class="band" id="strany" style="padding-top:0">
  <div class="wrap">
    <div class="viet">
      <div>
        <div class="sec-label">Флагман · Вьетнам</div>
        <h2 class="sec-title">Моя новая база — Вьетнам</h2>
        <p class="sec-intro">Переезжаю во Вьетнам и рассказываю изнутри: как оформить визу, сколько реально стоит жизнь, где селиться, какой интернет и как отсюда работать удалённо. Без глянца — как есть.</p>
        <div class="cta-row" style="margin-top:24px">
          <a class="btn btn-primary" href="/strany/vietnam">Открыть Вьетнам →</a>
          <a class="btn btn-ghost dark" href="/strany">Все страны</a>
        </div>
      </div>
      <div class="viet-card">
        <div class="viet-flag">🇻🇳</div>
        <ul class="viet-facts">
          <li><span>Виза</span><b>Виза / e-viza</b></li>
          <li><span>Бюджет</span><b>≈ $600–900/мес</b></li>
          <li><span>Климат</span><b>Тепло круглый год</b></li>
          <li><span>Интернет</span><b>Быстрый в городах</b></li>
          <li><span>Комьюнити</span><b>Растёт, есть наши</b></li>
        </ul>
      </div>
    </div>
  </div>
</section>

<section class="band" id="about" style="padding-top:0">
  <div class="wrap">
    <div class="sec-label">Почему мне можно верить</div>
    <h2 class="sec-title">Не теория — я живу этим</h2>
    <div class="benefits">
      <div class="ben"><div class="ck"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M3 12h18"/><path d="M12 3c2.5 2.5 3.8 5.6 3.8 9S14.5 18.5 12 21c-2.5-2.5-3.8-5.6-3.8-9S9.5 5.5 12 3z"/></svg></div><div><h3>Живу этим</h3><p>Сам перехожу на удалёнку и переезжаю во Вьетнам — рассказываю изнутри.</p></div></div>
      <div class="ben"><div class="ck"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M14.7 6.3a4 4 0 0 0-5.2 5.2L4 17l3 3 5.5-5.5a4 4 0 0 0 5.2-5.2l-2.3 2.3-2.7-.7-.7-2.7z"/></svg></div><div><h3>Строю публично</h3><p>Показываю процесс без прикрас: что работает, а что нет.</p></div></div>
      <div class="ben"><div class="ck"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M8 12.5l3 3 5-6"/></svg></div><div><h3>Проверено на практике</h3><p>Только то, что попробовал сам. Без инфоцыганства и пустых обещаний.</p></div></div>
      <div class="ben"><div class="ck"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="8" width="18" height="13" rx="1"/><path d="M12 8v13M3 12.5h18"/><path d="M12 8S9.8 3.5 7.5 5s.5 3 4.5 3zM12 8s2.2-4.5 4.5-3-.5 3-4.5 3z"/></svg></div><div><h3>80% пользы бесплатно</h3><p>Статьи, гайды и разборы — в открытом доступе.</p></div></div>
    </div>
  </div>
</section>

<section class="band" id="start" style="padding-top:0">
  <div class="wrap">
    <div class="sec-label">Маршрут</div>
    <h2 class="sec-title">От нуля до жизни где хочешь</h2>
    <div class="steps">
      <div class="step"><div class="n">01</div><h3>Выбери способ</h3><p>Нейросети, монтаж, контент или маркетплейсы.</p></div>
      <div class="step"><div class="n">02</div><h3>Освой с ИИ</h3><p>Нейросети ускоряют старт в разы.</p></div>
      <div class="step"><div class="n">03</div><h3>Найди клиентов</h3><p>Где искать заказы и как брать оплату.</p></div>
      <div class="step"><div class="n">04</div><h3>Живи где хочешь</h3><p>Выбери страну и работай из любой точки.</p></div>
    </div>
  </div>
</section>

<section class="final">
  <div class="final-bg"></div>
  <div class="wrap">
    <img class="logo" src="/logo.png" alt="pichugster">
    <h2>The sky has no borders<b>.</b></h2>
    <p>Начни создавать удалённый доход — и живи там, где хочешь.</p>
    <div class="cta-row">
      <a class="btn btn-primary" href="/pichugster-ai-start.pdf" download>Забрать гайд</a>
      <a class="btn btn-ghost dark" href="https://t.me/pichugster" target="_blank" rel="noopener">Telegram-канал</a>
    </div>
  </div>
</section>

<footer>
  <div class="wrap">
    <b>pichugster</b>
    <div class="links"><a href="/zarabotok">Заработок</a><a href="/strany">Страны</a><a href="/montazh">Монтаж</a><a href="/uslugi">Услуги</a><a href="https://t.me/pichugster" target="_blank" rel="noopener">Telegram</a></div>
    <div class="meta">© 2026 pichugster · the sky has no borders</div>
  </div>
</footer>

<script>
  document.querySelector('.burger').addEventListener('click',function(){
    var l=document.querySelector('.nav-links');
    var open=l.style.display==='flex';
    l.style.display=open?'none':'flex';
    if(!open){Object.assign(l.style,{position:'absolute',top:'56px',left:'0',right:'0',flexDirection:'column',gap:'0',background:'#fff',borderBottom:'1px solid var(--line)',padding:'8px 0'});
      l.querySelectorAll('a').forEach(function(a){a.style.padding='12px 28px'});}
  });
</script>
</body>
</html>
